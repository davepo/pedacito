[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pedacito"
version = "0.0.1a1"  # Alpha version, signals WIP
description = "A Python coding assistant for working with large codebases"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Dave Posocco", email = "davepo@gmail.com"}]
requires-python = ">=3.9"